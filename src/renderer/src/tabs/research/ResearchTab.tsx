import { Telescope } from 'lucide-react'
import Placeholder from '@/components/Placeholder'

export default function ResearchTab() {
  return (
    <Placeholder
      icon={Telescope}
      title="Research"
      subtitle="Multi-step research runs that search, read and synthesize sources into a cited visual report."
      milestone="M5"
    />
  )
}
